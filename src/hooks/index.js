import { useSelectFilters } from "./useSelectFilters";
import { useFilters } from "./useFilters";
import { useSort } from "./useSort";

const hooks = { useSelectFilters, useFilters, useSort };

export default hooks;
